export function RandomizeTiles() {
  let startList = [];
  for (let num = 1; num <= 18; num++) {
    startList.push(num);
  }
  for (let num = 1; num <= 18; num++) {
    startList.push(num);
  }
  //console.log('RandomizeTiles: ', startList);
 return startList;
};

